{
  "name": "client",
  "private": true,
  "scripts": {
    "build": "npm run build:iso",
    "build:iso": "cd apps/isomorphic && npm install && npm run build",
    "postinstall": "echo 'Skipping workspace installation for Vercel'",
    "dev": "turbo dev --filter=iso"
  }
}