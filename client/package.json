{
  "name": "client",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:iso",
    "build:iso": "cd apps/admin && npm run build",
    "dev": "turbo dev --filter=admin"
  }
}